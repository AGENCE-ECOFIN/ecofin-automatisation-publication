[pytest]
# Seuls les tests sous tests/ sont collectés (évite que app/api/test_generation.py soit pris pour des tests).
testpaths = tests
python_files = test_*.py *_test.py
norecursedirs = .git venv __pycache__ node_modules
